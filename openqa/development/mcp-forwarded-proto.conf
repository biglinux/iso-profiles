<Location /mcp>
    # Docker's published-port proxy is not seen as localhost by openQA. The
    # container is published only on host loopback, so this local hop is the
    # HTTPS-equivalent required for personal access token validation.
    RequestHeader set X-Forwarded-Proto "https"
</Location>
